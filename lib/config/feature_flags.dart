


/// Feature flags for My Drivers app
class FeatureFlags {
  // Scheduling feature flag (M1)
  static const bool schedulingEnabled = true;

  // Driver QR poster generation feature flag (M1)
  static const bool driverQRPosterEnabled = true;
}

